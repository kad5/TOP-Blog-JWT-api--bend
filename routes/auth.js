const { Router } = require("express");
const router = Router();

router.post("/sign-up", () => {});
router.post("/log-in", () => {});
router.post("/log-out", () => {});
router.post("/reset-password", () => {});

module.exports = router;
